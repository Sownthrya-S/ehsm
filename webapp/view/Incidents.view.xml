<mvc:View controllerName="ehsm.controller.Incidents"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Page title="Incident Management" showNavButton="true" navButtonPress="onNavBack">
        <content>
            <Table id="idIncidentTable" items="{incidentModel>/}" growing="true" growingThreshold="10">
                <headerToolbar>
                    <OverflowToolbar>
                        <Title text="Incidents" level="H2"/>
                    </OverflowToolbar>
                </headerToolbar>
                <columns>
                    <Column>
                        <Text text="Incident ID" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="Plant" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="Description" />
                    </Column>
                    <Column minScreenWidth="Desktop" demandPopin="true">
                        <Text text="Category" />
                    </Column>
                    <Column minScreenWidth="Desktop" demandPopin="true">
                        <Text text="Priority" />
                    </Column>
                    <Column>
                        <Text text="Status" />
                    </Column>
                    <Column minScreenWidth="Desktop" demandPopin="true">
                        <Text text="Date" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{incidentModel>IncidentId}" />
                            <Text text="{incidentModel>Plant}" />
                            <Text text="{incidentModel>IncidentDescription}" />
                            <Text text="{incidentModel>IncidentCategory}" />
                            <ObjectStatus text="{incidentModel>IncidentPriority}" state="{path: 'incidentModel>IncidentPriority', formatter: '.formatPriority'}" />
                            <ObjectStatus text="{incidentModel>IncidentStatus}" state="{path: 'incidentModel>IncidentStatus', formatter: '.formatStatus'}" />
                            <Text text="{path: 'incidentModel>IncidentDate', type: 'sap.ui.model.type.Date', formatOptions: { pattern: 'yyyy-MM-dd' }}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>
